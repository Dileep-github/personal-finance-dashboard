// Intentionally minimal — the renderer talks to the backend purely over
// HTTP (fetch), so no contextBridge APIs are needed today. Kept as a
// placeholder hook point (e.g. exposing an app version later) with
// contextIsolation left on.
